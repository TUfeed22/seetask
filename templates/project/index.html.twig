{% extends 'base.html.twig' %}


{% block page_header %}

    <div class="page-heading">
        <h3>{{ title }}</h3>
    </div>

{% endblock %}

{% block body %}

    <section class="section">
        <div class="col-12">
            <div class="card">
                <div class="card-body">
                    <div class="buttons">
                        <a href="{{ path('app_project_add') }}" class="btn btn-outline-success">Новый проект</a>
                    </div>
                </div>
            </div>
        </div>
        <div class="card">
            <div class="card-body">
                {{ component('Table', {
                    columnNames: '#,Наименование,Кол-во задач,Статус,Создатель',
                    entity: 'project'
                }) }}
            </div>
        </div>

    </section>


{% endblock %}

