{% extends 'base.html.twig' %}

{% block body %}
    <div class="content-wrapper container">
        <div class="page-heading">
            <div class="page-title">
                <div class="row">
                    <div class="col-12 col-md-6 order-md-1 order-last">
                        <h3>{{ title }}</h3>
                    </div>
                </div>
            </div>
            <section class="section">
                <div class="card">
                    <div class="card-header">
                        <h4 class="card-title">Заголовок блока</h4>
                    </div>
                    <div class="card-body">
                        Информационная панель, тут будет общая сводка по задачам текущей учетки.
                    </div>
                </div>
            </section>
        </div>
    </div>

{% endblock %}
